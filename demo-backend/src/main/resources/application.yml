spring:
  h2:
    console.enabled: true
  datasource:
    url: jdbc:h2:file:./temp/data
    username: sa
    password: password
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate.ddl-auto: create-drop
